# 小さな身体操作と毎step判断：設計・実装計画

作成: 2026-09-23。状態: **順序1〜5と第9節の初回条件、遠方開始のN候補を小規模に試行**。本書の設計と初回予算は当初案として残す。
初回結果は第10節、継続試行と現在の障害は第11〜13節、全runの条件は [実験メモ](ITERATION_LOG.md) に記録した。

## 1. 採用する方針と既存計画との関係

プリミティブを「局所的な身体状態を変更する制御操作」とする。状態を読む小型モデルが
毎制御step、有限個の操作から直接選択する。文章生成や外部判断APIは使わない。
Jev/Layaから参考にするのは、型の決まった判断を一度の推論で返す思想であり、
Laya本体・重み・SDKの組込みやその学習手法の再現は本計画に含めない。

`navigate_to`、`grasp_lift`、`move_held_to`は銀行の初期プリミティブから外す。
これらは、小操作の選択を繰り返して生じる課題レベルの行動、または比較用の既存方策として扱う。
接近・下降・把持・持上げという教師stageを、そのまま銀行のIDに置換する設計でもない。
同じ手先並進を、接近・持上げ・運搬で使い回す。

本計画が [RESEARCH_PLAN.md](RESEARCH_PLAN.md) 第4節の粒度・選択方式と、
「既存移動方策の銀行化から始める」順序を更新する。第5〜7節の追加・圧縮・解放・
再利用・保持という研究目的と計測方針は維持し、具体的な課題順は本書第9節で読み替える。
旧実験のコード・checkpoint・run・測定結果は保存する。旧Phase A–Cの内部演算とは区別する。

## 2. 実装済みの土台と不足

| 現行の構成 | 再利用する部分 | 新たに必要な部分 |
|---|---|---|
| `arm_ik.py` の `ArmIKPolicy` | 世界/root座標変換、IK、関節範囲・机距離の検査、指令生成 | 固定stage列から独立した、保持目標を受け取る追従器 |
| `synchronize_pick_place()` | 物理状態から選ぶ手設計回復教師 | 小操作ラベルへの変換と、そのラベルでの実rollout |
| `operation_bc.py` | 実軌跡の学習・保存・単独推論の仕組み | 連続11出力の回帰とは別の、プリミティブ分類器 |
| `operation_dagger.py` | 行動前の教師ラベルと実行行動の分離 | 実行器の内部状態を含む教師問い合わせ |
| `runner.py` の `external` | `reset/action/after_step`、NPZ・JSONL・出自記録 | 判断・目標管理・時間計測を持つ外部方策adapter |
| 台車BC・蒸留 | 比較用の既存能力と圧縮の実績 | 小さな進退・旋回用の局所制御器、共通sceneでの評価 |

既存操作BCはstate54からarm/gripper/bodyの11次元指令をまとめて学ぶ。
小操作ごとの学習済み重みではない。既存runの制御は20 Hz、物理計算は100 Hz。
操作BCは探索条件で2/3目標到達、未使用seedで0/5、到達後の保持も未達という記録であり、
本設計による改善はまだ測っていない。詳細は [実験メモ](ITERATION_LOG.md) を参照。

## 3. プリミティブの定義

プリミティブは「保持された制御目標の一部を更新する操作」と「その目標へ追従する制御」で構成する。
1回の選択は1制御stepで反映でき、その後も目標を保持する。固定の長時間実行は要求しない。
生の関節指令は実行器が毎step生成し、判断器は関節ごとの数値を直接出力しない。

| 種類 | 目標への作用 | 初期引数案 | 維持するもの |
|---|---|---|---|
| `translate_hand(delta_p)` | 実測手先位置を基準に局所位置目標を更新 | ±X/Y/Zに1 cm | 手先の目標姿勢、指幅 |
| `rotate_hand(delta_r)` | 実測手先姿勢を基準に局所姿勢目標を更新 | 3軸の正負に3度 | 手先の目標位置、指幅 |
| `advance_base(delta_s)` | 実測台車位置・yawを基準に短距離目標を設定 | 前後2 cm | 指幅、腕・胴体の保持姿勢 |
| `turn_base(delta_yaw)` | 実測yawを基準に旋回目標を設定 | 左右3度 | 指幅、腕・胴体の保持姿勢 |
| `set_gripper(width)` | 指幅目標を置換 | 開く／閉じる | 手先と台車の目標 |
| `hold()` | 手先と台車の運動目標を現在の実測姿勢へ置換 | なし | 指幅目標 |

引数値は実験の初期案であり、適切な刻み幅の実測結果ではない。configとmanifestへ保存する。
指幅は上流Fetch controllerの定義・上下限に合わせる。閉鎖指令と把持成立は別の状態である。
最初は胴体昇降を独立した選択肢にせず、既存の腕・胴体協調IKに含める。

### 座標系と目標の固定

- 並進・回転の軸は操作選択時のロボットroot座標。並進量はm、回転量はrad。
- 並進目標は `p_world_measured + R_world_root * delta_p` とし、世界座標で固定する。
- 回転はEuler角を加算せず、root軸の回転ベクトルを世界軸へ変換し、対応する回転を
  実測手先姿勢へ左から合成する。quaternionの順序と正規化をschemaへ記録する。
- 同じ並進IDの再選択も「現在の実測位置からの新目標」とする。古い目標への差分累積はしない。
- 既存目標へ追従するだけの選択は別IDの `continue`。目標・開始時刻を更新しない。
- `hold` は速度が瞬時に0になる指令ではない。保持中も速度・接触・把持の変化を観測する。

### 有限の候補集合

完全版の初期案は、並進6 + 回転6 + 台車4 + 指2 + `continue/hold` 2 = **20候補**。
初回は台車を停止し、手先姿勢を固定して、並進6 + 指2 + 管理2 = **10候補**だけを使う。
固定姿勢はreset時の実測姿勢を既定にする。把持診断のため初期姿勢を変える場合は、
準備動作・初期化方法・その収集費を記録し、その結果を任意姿勢からの把持能力と呼ばない。
姿勢固定が失敗原因なら、全体の成功を待たず回転候補を先行して追加する比較も行える。
これは同じ6種類の操作を離散化した集合であり、20個のネットワークを作る意味ではない。
各checkpointにID順序・引数・active集合のversionを保存し、候補追加時に旧IDを付け替えない。
実装では10候補のID 0〜9を固定し、回転6候補をID 10〜15、台車4候補を
ID 16〜19へ追加した。上記の種類別の表記順ではなく、このID順を使う。
現行の学習checkpointは16候補専用で、台車4候補の選択を学習したものではない。

軸方向への離散化で斜め移動が遠回りになる、1 cm刻みで接触近傍が粗すぎる場合は、
刻み幅だけを変える比較から始める。大小の候補や連続引数headは後続案とし、初版へ同時投入しない。

## 4. 判断器と実行器

```text
数値観測・最終目標・実行器の保持目標・直前の操作
    ↓ feature adapter
小型判断ネットワーク → 各候補のlogit
    ↓ 有効候補mask → 選択ID
実行器 → 目標の継続／更新 → IK・PD・台車制御
    ↓ 全身13次元action
ManiSkillの1制御step → 次の観測・診断・ログ
```

### 判断器の入力と出力

初版は幅64の2層MLP + 候補数分のlogitを出すheadを候補とする。
隠れ層の幅・活性化・正規化はconfigに固定し、学習データだけから正規化統計を作る。

入力はqpos/qvel、手先・物体・最終目標の相対位置と姿勢、把持状態、利用可能な速度、
保持中の手先/台車/指目標、目標の有効フラグ、直前ID、各目標の経過時間とする。
現在のstate54に含まれない量は専用adapterで追加し、順序・単位・座標系をversion管理する。
教師stage・教師の内部目標・未来のsuccessは推論入力へ渡さない。
保持目標は判断器自身の操作から生じた情報であり、教師情報とは区別する。
初版の把持・物体状態はシミュレータの状態観測であり、実機の知覚能力を示すものではない。

初期推論は有効候補のargmax。モデルに「段階順序」を直接教え込むmaskは置かない。
確率分布・上位差・エントロピーは診断として保存し、確信度を成功確率や能力不足の証明としない。
履歴不足の観測が出た場合に限り、短い観測履歴または小型GRUを一案ずつ比較する。

### 実行器が持つ状態

手先位置/姿勢目標、指幅目標、台車位置/yaw目標、腕・胴体保持関節値、各目標の
開始時刻・有効性・到達状態・失敗理由を持つ。episodeのresetと操作の切替を別処理にする。
同一sceneで切り替え、環境・物体・controller・経過stepをresetしない。

1. `continue` は全ての有効な目標を保持する。reset直後は現在姿勢を初期保持目標にする。
2. 並進/回転は対応する目標成分だけを置換する。指幅は別に保持する。
3. 指の操作は腕の追従目標を消さない。`hold` でも指は開かない。
4. 台車と腕の同時運動は初版では扱わない。拡張時の台車操作開始では腕・胴体を実測関節値で
   保持し、未完了の手先目標を無効化する。腕操作開始では台車目標を現在姿勢へ置換する。
   この中断を記録し、台車移動中に世界座標の旧手先目標へ引き戻すことを防ぐ。
5. 全身actionは実行器1か所で生成する。既存台車方策のrest復帰・固定指幅を流用しない。

非台車部の保持を含め、腕0:7・指7:8・body8:11・base11:13の対応を実controllerから確認する。
`hold` を全13次元のゼロ指令で代用しない。指には保持した幅に対応する指令が必要になる。

### 制約・中断・時間

関節範囲・数値の有限性・既存机経路検査を共通実行器に残す。既存の机検査は有限サンプルの
AABB検査であり、連続衝突判定・自己衝突・指開閉の全経路を保証しない。
安価な前提検査を候補maskに使い、選択後に必要なIKと経路検査をする。
毎step全候補のIKを解くことは初版の既定にしない。

選択が実行不能なら理由を保存し、腕・台車を保持へ移し、指幅は維持する。次stepで再判断する。
全タスクを停止せず、同一失敗の反復は停滞として記録する。把持落下・接触などの状態は毎step更新する。
各目標にconfig由来の時間上限を持ち、到達/時間切れを通知する。時間切れで新しい目標を
自動累積せず保持へ移す。到達後の`continue`はその目標の保持になる。
位置/姿勢/速度の到達許容値、指の到達判定と把持判定、時間上限は別の設定にする。
刻み幅より大きい到達許容値で新目標を即時完了扱いにしない。長時間の把持保持では、
指の到達待ちが時間切れになっても閉鎖目標を維持し、タイマーだけで開放しない。

最初は固定の最小継続stepを強制しない。頻繁な正負反転が出た場合にのみ、反転への
短い抑制や切替コストを追加比較する。制約違反・無効目標による中断は抑制しない。
切替規則が課題手順を代行していないか、override率と理由から確認する。

## 5. 教師と学習データ

手設計の小操作selectorを先に動かし、同じ実行器で学習selectorと比較する。
既存IK教師の局所目標から、例えば最も大きい位置誤差軸を選ぶ方法を初期教師とする。
現在目標への追従中は`continue`、指の目標を変えるときだけ指IDを出す。
既存の把持再同期も使えるが、教師版・閾値・目標生成法・同点選択規則を保存する。
動作が成り立たないときは刻み幅/教師/制御を調べ、分類器の学習だけを続けない。

既存の11次元指令を単純に最近傍IDへ丸めたラベルを正解と仮定しない。
同じ目標へ近づく操作が複数あること、指開閉と腕移動が教師では同時であることを考慮する。
オフライン変換は診断用に限定し、初版教師のラベルは小操作を実行した実軌跡で検証する。

学習は分類の交差エントロピーから始める。`continue`等の占有率とID別誤差を記録し、
均等化が必要なら抽出比率だけを変える。RLや特別な確率校正法は初版の必須要件にしない。
learner状態で教師に問い合わせる際は、その時点の実行器目標・履歴も渡す。
無効な教師ラベルは有効フラグと理由を記録して除外し、勝手に正解`hold`へ置換しない。
実行したID/actionと教師IDを分離し、教師が実行時fallbackを兼ねた成績を単独推論と呼ばない。

学習/検証はepisode・seed単位で分け、隣接stepをランダム分割した分類精度だけで評価しない。
分類精度だけでなく、learner自身のrolloutで把持・持上げ・到達・保持を測る。

## 6. 保存する情報と速度の評価

既存NPZの `observations[t] → actions[t] → observations[t+1]` を維持する。
既存観測の意味を上書きせず、step診断へ専用versionの判断入力・内部状態を追加する。
行動前情報は`action()`時にコピーし、`after_step()`で得る行動後情報と名前を分ける。

- manifest: プリミティブ表・ID順・刻み幅・座標系・mask/中断規則、モデル/実行器/教師version、
  checkpoint/hash、入力schema、制御/物理/判断周期、依存・CPU/GPU・thread数。
- step: 行動前の入力と保持目標、logit/確率/mask、提案ID・採用ID・教師ID/有効性、
  目標の更新前後、override理由、IK結果、到達/時間切れ、実送信13次元action。
- 結果: 把持・持上げ高さ・初回/最終成功・到達後20 step保持、接触、停滞、
  ID変更数・同じIDでの目標更新数・正負反転数、無効選択率・override率。
- 費用: 環境step、教師問い合わせ、更新数、wall time、selectorと実行器を含むパラメータ/
  tensor/checkpoint容量、測定時点付きRSS。コードで固定した制御器の寄与も明記する。

判断器単体、入力生成、候補検査・目標更新、IK/指令生成、env.step、ログ保存を別に計時する。
合計処理時間のp50/p95/最大値と50 ms超過率も残す。warm-upと初期ロードを定常推論から分ける。
GPUを使う場合は同期を含む時間を記録する。候補数・batch=1・CPU thread数を揃える。

初版は制御20 Hzと同じ毎step判断。同期runnerでは推論中に物理時刻は進まないため、
rollout完了を実時間20 Hzの達成と呼ばない。50 msは現行周期から来る比較予算であり、
速度未達でも研究を止めるゲートにはしない。速度調整が必要なら2/5 stepごとの判断を
別条件で比べ、その間も実行器は毎step追従・制約検査を続ける。

## 7. 実装の順序と初回予算

全段階とも専用WSL/Python環境、CPU・単一Fetch・状態観測から始める。
出力先は毎回新しいrun。以下は実装時の初期予算案であり、今回実行した実績ではない。
各段階の結果で次の一点を選び、全条件の網羅探索は行わない。

| 順序 | 実装・試行 | 初回予算案 | 残す成果物 |
|---|---|---|---|
| 1 | 10候補の定義、目標管理、IK追従器、外部方策adapter。短い手動操作で並進・継続・指・保持を確認 | 3 episode × 最大100 step = 300環境step、学習0 | 目標が累積しないこと、指目標・物理状態の連続性、時間計測 |
| 2 | 同じ実行器上の手設計selector。把持・持上げ・目標保持を試す | 3 episode × 最大350 step = 1,050環境step、学習0 | 教師の小操作ラベル、無効ラベル、刻み幅/制御の失敗 |
| 3 | 小型MLPの学習・checkpoint単独rollout。必要ならDAggerを1 round追加 | 初期3,000更新 + 3 episode/最大1,050 step。追加roundは収集1,050 step + 3,000更新 + 評価1,050 step | 単独推論、教師との同seed比較、分類と閉ループ誤差 |
| 4 | 設定固定後の未使用条件評価。主な失敗が分かれば一要因だけ変更して探索を再開 | 5 episode × 最大350 step = 最大1,750 step/方式 | 成功・保持・切替・速度・費用、未使用条件の失敗 |
| 5 | 回転と台車4操作を追加し20候補へ。共通sceneで腕/台車の中断と指保持を検証 | まず3 episode × 最大350 step = 1,050 step、学習は別途明記 | 観測adapter、ID互換性、同sceneの移動と操作 |

順序1〜3は追加DAggerなしなら最大2,400環境step・3,000更新、追加時は
最大4,500環境step・6,000更新。教師問い合わせと機能テストの環境stepは別欄に記録する。
順序4以降は別予算。未使用seedは既存runを調べて選び、1200〜1204など使用済みseedを再利用して
独立評価と呼ばない。少数例の成功率を一般的な性能保証にしない。

### コードへの対応案

- `primitives.py`（新規案）: ID表、引数、契約、保持目標の状態。汎用plugin基盤には広げない。
- `primitive_policy.py`（新規案）: 実行器、手設計selector、runner用adapter。
- `primitive_learning.py`（新規案）: 特徴量schema、分類器、学習・再ラベル・checkpoint。
- `arm_ik.py`: 目標追従部分を分離して共用。既存stage教師の入口と挙動は維持する。
- `runner.py` / `__main__.py`: 既存external経路を優先し、必要な設定・学習入口だけ追加する。
- `configs/`（新規config）: 初版10候補、全20候補、刻み幅、最大時間、モデル・判断周期。

この表は当初案である。実装済みの入口と実行条件は各runのmanifestと
`scripts/run_fetch_primitives.py`、`primitive_learning.py` のCLIを参照する。
既存checkpointを新schemaとして読み替えず、明示的に互換性を検査する。

## 8. 検証と比較

新しい一機能の入口から成果物までを確認する統合テストを原則1本とし、
候補名の数だけunit testを増やさない。初版の実環境rolloutで、目標更新/継続/保持、
指幅保持、episode境界、保存ログと実送信actionの対応を確認する。
学習入口を追加したら同じ機能テストへ短い学習・再読込・教師なし実行を拡張する。
既存IKの共用部分を変更する場合は、既存腕IK機能テストも対象にする。
タスク成功率をテストの合格条件にせず、未実行や障害を明記する。

最初の主比較は「同じ小操作群・実行器を使う手設計selector」と「学習selector」。
既存連続BCも同じ開始条件で測るが、IKを使う提案方式との制御器の違いを明記する。
細粒度化自体の効果を調べる段階では、同じIK追従器上の粗いstage目標方式も対照に置く。
全対照を初日に揃える必要はない。成功率の差を判断方式だけの効果とするには、
観測、教師情報、制御器、収集費、開始条件の違いを分けて比較する。

失敗は判断誤り、局所制御/IK不成立、刻み幅不足、保持目標の不整合、接触/把持失敗に分ける。
手設計selectorでも失敗する条件を、分類器の容量だけで解決しようとしない。

## 9. APCの追加・圧縮・解放へ進む条件の整理

固定の小操作群でselectorを学ぶ初版は、組合せ学習の実験である。
それだけでは新プリミティブの獲得・銀行増設・temporary解放を示さない。
selectorの蒸留と、行動プリミティブ本体の獲得・蒸留も別の成果として記録する。

まずKを小操作で解ける既知条件、Cを同じ操作集合での別配置・目標・操作順として測る。
N候補で失敗したら、(a) selectorのみの修正、(b) 刻み幅/目標表現の修正、
(c) 局所制御器の不足を小さく比較する。低確信度や失敗だけで(c)と断定しない。
20候補への手動拡張も、自動能力獲得として数えない。

(c)が有力な場合に、既存プリミティブを凍結し、局所制御のtemporaryを学習する。
例は接触近傍での追従・把持保持を改善する小制御器だが、現在未獲得と確定した課題ではない。
teacherの固定手順を丸ごと新プリミティブへ戻す前に、局所的な開始範囲・作用・時間上限を定義する。
小型candidateへ蒸留し、新IDまたは明示versionとして登録する。selector更新費も記録する。
独立プロセスでtemporary/optimizerを読み込まず再利用し、R条件と過去K/Cの性能を比較する。

銀行version、凍結範囲、追加/置換履歴、容量、学習費、selector変更による退行までを対象とする。
厳密な「既存集合では不可能」の証明や成功率閾値を次の試行の条件にはしない。
観測された不足と未解決の仮説を残し、実装と小さい試行を往復する。

## 10. 2026-09-23 初回実装・試行の結果

`primitives.py`、`primitive_policy.py`、`primitive_learning.py` と `scripts/run_fetch_primitives.py` を追加。
既存 `ArmIKPolicy` のIK、関節範囲、机経路確認とrunnerの外部方策経路を利用した。
10候補の手動列は `runs/primitive-manual-20260923-a/` で3 episode/300 stepを完了。
並進目標の更新、継続、閉開指、保持を同一sceneで確認した。
この手動列は物体を狙わないため成功0/3。固定姿勢の教師は
`primitive-teacher-20260923-a/` の1,050 step中655 stepでIK解が範囲・机条件により拒否された。

回転6候補を足した同seed比較では拒否0/1,050 stepとなり、一時把持を3/3で観測。
把持位置の許容幅を狭め、step上限だけを500、600へ順に延ばした。
最終教師run `primitive-teacher-rotation-20260923-d/` はseed 1300〜1302、
3 episode/1,683 step、目標到達と到達後20 step保持が2/3。
残るseed 1302の最短目標距離は約3.0 cm。これらは全て探索に使用したseed。

教師runのうち2 episodeを学習、1 episodeをepisode単位の検証に使った。
状態と保持目標から16 IDを選ぶ幅64×2のMLPは8,336パラメータ、3,000更新。
同じ3 seedでのcheckpoint単独rolloutは成功0/3。learner状態1,800 stepへ
手設計selectorを問い合わせると、提案IDの不一致は1,014/1,800。
実行行動は教師問い合わせなしのrunと全episodeで一致し、教師IDは別保存した。
追加3,000更新の1-round DAggerも成功0/3で把持は2/3→0/3に退行。
元教師データだけでID抽出比率を平方根逆頻度へ変えた比較も3,000更新・成功0/3。
各条件のrun、損失、ID分布、失敗は実験メモに記載した。

今回のrunner実験は合計30 episode/13,833環境step、学習9,000更新。
実環境の統合テスト `tests/test_primitive_feature.py` 1本は通過（テスト内270 stepは別計上）。
速度ログはselector、目標更新、IK/指令生成だけで、env.stepとログ保存を含まない。
初期MLPのselector p50/p95は0.587/0.990 ms、IK/指令生成は3.264/13.279 ms。
同期runnerでの実測であり、実時間20 Hzの保証ではない。

**初回試行時の明確な障害:** 教師が解ける探索条件でも学習selectorが閉ループで解けない。
`continue`が教師1,683ラベル中1,255を占め、learner側には異なる状態と目標が生じる。
分類誤差、目標状態の表現、固定操作刻み、接触後の制御の寄与は未分離。
初回時点では未使用seedの独立評価、台車4候補、銀行追加・temporary解放へ進んだ成果とはしない。
次は失敗軌跡の局面別ID誤りと、教師の保持目標を含む入力の識別性を調べる。

## 11. 2026-09-23 継続試行：相対特徴、未使用条件、20候補実行器

第10節の障害から、v1の正規化ではほぼ定数のquaternion成分が過大に拡大されることを確認した。
標準偏差下限を0.01へ上げ、root系の手先→物体、物体→目標、手先→保持目標を使う
version付き相対特徴v2を追加した。教師episode単位の検証accuracyは64.3%から70.8%へ
上がったが、同じ探索seedの閉ループ成功はいずれも0/3だった。全教師episodeを学習し、
learner状態への教師ラベルを2回追加すると最終成功1/3となった。成功後20 stepの観測完了は
連続成功を意味しないため、各比較は初回成功と最終成功を別に数える。

未使用seed 1500〜1504では固定モデル0/5、教師3/5が最終成功。結果を見た後、この組を
探索データへ移した。1500教師で学習したモデルは次の未使用1600〜1604で1/5、教師2/5。
1600も探索へ移して両教師を学習したモデルは、未使用1700〜1704で3/5、教師4/5。
旧教師1300、1500、1600とlearner状態の2収集runを統合した別モデルは、次の未使用
1800〜1804で0/5、教師1/5。各組は5 episodeだけで、条件間で成績が安定したとは言えない。
特に1802は教師が成功し、統合モデルは把持に至らない。分類器の訓練accuracyでは
閉ループ汎化を説明できず、教師自身が解けない初期配置も混在する。1802では序盤の回転は
概ね類似するが、300〜400 stepの接近で教師だけが指閉鎖まで進む。次はこの局面の判断と、
それ以外の教師失敗を分けて検査する。追加の再学習roundを成功の証拠なしに
自動的な能力獲得とは呼ばない。

実行器には台車前進/後退2 cm、左右旋回3度をID 16〜19として追加した。台車開始時は
腕・胴体の実測関節を保持して旧手先世界目標を無効化し、腕再開時は台車目標を実測姿勢に
置換する。同一sceneの手動列3 episode/690 stepで全4候補を各回実行し、腕→台車→腕の
中断、指幅保持、実測の戻り、送信actionとログの一致を確認した。サンプル点による机経路
検査を各候補・stepへ適用し、記録した全stepで机接触力は0 Nだった。連続衝突保証や
台車を必要とする課題の成功実験ではない。16候補のMLPを20候補と偽ってロードせず、
台車の判断器は未学習と明示している。

今回追加したrunnerは76 episode/40,850環境step、学習27,000更新。既存の実環境統合テスト
1本を20候補まで拡張し、1 passed（450テストstep、上記から除外）。全runは保存し、
探索/未使用の扱い、source hash、送信行動、失敗も [実験メモ](ITERATION_LOG.md) に残した。

## 12. 2026-09-23 次段階：姿勢診断、台車連鎖と条件別回復

固定MLPのseed 1802で実訪問状態へ教師を問い合わせ、送信action 600 stepが問い合わせなしと
全件一致することを確認した。学習器の手先pitchは約12.3度に留まり、教師の15±2度から外れる。
後半の教師は回転ID 12をほぼ連続で選ぶが、モデルは主に`continue`を選んだ。これを含む
600 stepをDAggerへ追加しても、sqrt逆頻度・均等抽出・この収集だけ10 step間隔での抽出の
3条件は全て同seedで最終成功0/1。近接した教師ラベルの単純追加で解決する障害ではない。

固定MLPのpitchだけを教師規則で補正する診断用hybridを作った。seed 1802では600 step内に
把持へ至り、上限700 stepでは初回成功step 611を観測したが最終成功はfalse。
成功後20 stepの教師/実行ID差は3件で、物体が許容距離2.5 cmを再び超えた。
探索seed 1800〜1804の600 step比較では、元MLP・hybridとも最終成功0/5。
hybridは教師の姿勢規則を実行時に使うため学習selectorの性能として数えない。

台車を使った実タスク連鎖で、約4 cmの前進後に`base_link`と机の接触を発見した。
従来の机経路検査は腕リンクのみで、この接触を検出できなかった。台車移動時の候補・毎step
検査へ`base_link`を追加し、2回目以降の前進を拒否するようになった。初回の接触runも保持。
安全な前進1回（約2 cm）から腕操作へ通常切替する手設計selectorを実装し、探索seed
1702〜1704では最終成功2/3、全記録stepの接触0 N、拒否0。台車なし教師は同seedで3/3。

未使用seed 1900〜1904の600 stepでは、台車なし教師4/5、35 step待機対照1/5、
前進1回の連鎖1/5。上限を700 stepへ延ばすと待機対照4/5、前進連鎖2/5となった。
時間切れだけでなく、台車前進後の腕経路拒否がseed 1901/1903で493/380 step続いた。
`base_link`を台車移動時だけ検査する変更ではこの拒否は減らず、腕側の経路問題だった。
既存ID 17の後退を、連続した腕経路拒否5回で一度だけ選ぶ手設計回復を追加。
探索seed 1900〜1904では最終成功4/5、接触0 N。未使用seed 2000〜2004では
回復付き3/5、待機対照4/5で、後退は3 episodeに発動した。回復は同一sceneで台車→腕→台車→腕
を行い、物体・controller・経過stepをresetしない。

これは20候補の手設計選択・安全な中断・回復の実証である。学習済み20候補selector、
新プリミティブ獲得、圧縮、temporary解放の証拠ではない。現行PickCubeの物体配置は腕で
到達可能な例を多く含み、台車前進は机との接触余裕が約2 cmしかない。台車が必要なN条件を
作るには物体だけでなく机・ロボットの相対配置と安全な移動経路の設計が必要。
その条件がないまま台車を常時選ぶ方策を学習・登録しても能力追加とは呼べない。

今回のrunnerは67 episode/40,719環境step、3モデル各3,000の計9,000学習更新。
実環境統合テスト1本は最終 **1 passed**（850テストstep、別計上）。失敗run、source hash、
IK拒否と接触、条件別の初回/最終成功、手設計規則は [実験メモ](ITERATION_LOG.md) に保存した。

## 13. 2026-09-23 遠方開始のN候補と20 ID学習selector

上流Fetch/PickCubeの机・物体・目標分布を維持し、各reset後のロボットrootだけを20 cm後方へ
置く `APC-FetchPickCubeFar-v1` を追加した。台車の前進経路に余裕がある同一sceneで、
既存ID16の前進を繰り返してから同じ腕操作へ切り替える。seed1901〜1903、900 step上限と
初回成功後20 stepの同条件比較では、台車なし教師は最終0/3、30 stepごと10回前進する
手設計列は最終3/3。台車なしの手先・物体の最短距離は0.159〜0.292 m、腕経路拒否は
各602〜710 stepだった。移動列は台車経路拒否・腕経路拒否・机接触を記録した範囲で0。
この条件では**台車移動が観測上有効**だが、すべての初期配置で必須という証明ではない。

初回列は別seed2101〜2103で最終1/3となり、教師自身の可解範囲にも差があった。
台車の保持目標誤差と経過stepで次の前進を決める `base_ready_pick_v1` は探索seed1901〜1903で
最終3/3、別seed2301〜2303で2/3、2401〜2403で初回3/3・最終2/3。後者のseed2402は
成功後20 stepを観測し終えた時点で成功を失った。台車目標の完全到達後だけ腕へ切り替える
診断版v2は同じ探索seedで1/3となり、切替位置が重要だった。有限stepでの計測であり、
連続時間の非接触保証ではない。

20候補のMLP headと台車状態を含むv3特徴、台車目標到達・切替境界を含むv4特徴を実装した。
30 step間隔教師で学習したv3初版は未使用seed2101〜2103で最終0/3、教師を追加して
ID抽出の偏りを弱めた版も新seed2201〜2203で0/3。初版は台車を約22 cm進めた後も
前進を選び、安全検査に各449回拒否された。v4の状態ベース教師で学習すると新seed
2301〜2303で0/3だが、台車11回の選択と腕への切替は3/3で実行、台車拒否・机接触は0。
腕判断が残る失敗となった。

20 ID対応教師を学習器の訪問状態へ問い合わせる経路を追加。seed2303で900/900 stepの
送信行動が問い合わせなしrunと一致し、900件の有効ラベルを保存した。モデルIDとの不一致は
653件。これをDAggerへ追加し3,000更新したモデルも新seed2401〜2403で最終0/3。
台車11回と切替は3/3、台車拒否・机接触0、腕経路拒否15〜226 step、把持は1/3。
同seedの手設計教師は初回3/3・最終2/3。この比較は学習20 ID selectorの未解決を示す。
seed2303の教師問い合わせでは、台車局面183 stepのIDは全件一致し、腕局面717 step中
653件が不一致。教師は回転ID13を662回選び、モデルは主に継続・保持を選んだ。
そこで20 IDモデルの腕pitchだけを教師規則で補う診断hybridをseed2403で実行。
最終pitchは16.2度まで補正されたが成功0/1、腕経路拒否271 step、把持なし。
姿勢の誤選択だけでは説明できず、hybridを学習済み能力として数えない。
台車・腕の既存局所制御器で成功例があるため、現段階で不足を新プリミティブ本体の
獲得問題と断定しない。temporary学習、candidate蒸留、銀行増設・解放は未着手。

この段階のrunnerは14 run/36 episode/30,713環境step、20候補モデル4本各3,000の
計12,000学習更新。台車を許可したrunの机接触計測は全記録stepで0 N。最後の実環境統合
テスト1本は **1 passed**（1,390テストstep、runner総計外）。seedの再使用・対照の範囲、
失敗runを含む個別結果は [実験メモ](ITERATION_LOG.md) に残した。次は腕局面の`continue`と
軸移動の誤選択を、教師が成功した同seedの訪問状態ごとに分解し、selector表現と収集を試す。

## 14. 2026-09-24 幾何特徴、ラベル不足と学習した決定木

前回MLPのseed2401〜2403を再実行し、教師問い合わせの有無で2,700件の送信actionが
全件一致した。台車局面549 stepは教師と全件一致。一方、2401では教師の上向き回転ID12を
継続/並進へ置き換え、2402では上昇ID4を継続へ置き換える誤りが多かった。
元の学習3,155 sampleにはID12が0件だった。単なる容量不足と決めず、ラベルの欠落と
幾何量の表現を分けて比較した。

v5特徴へ手先pitch、保持目標との角度/位置誤差、水平接近距離、把持点距離、
root系の軸誤差の絶対値・軸間差を追加。把持高さ12 mm・接近高さ12 cmは教師と共有する
手設計のtask priorである。teacherのstageや正解ID、実行時の教師判断は入力しない。
同じ元データのまま特徴だけを変えたMLPは2401〜2403で最終0/3。
今回の問い合わせ2,700 sampleを加えるとv4/v5とも1/3となった。
教師の別配置と再訪問状態を合わせた12,789 sampleのv5 MLPも、次の2501〜2503では0/3。
特徴追加単独の改善は確認できず、不足ラベルの追加には同seedで改善を観測した。

教師の小さな境界で判断が切り替わる性質に合わせ、同じ入力とラベルから重み付きGini分岐を
学ぶCARTを追加。追加ライブラリを入れずNumPy/PyTorchでCPU学習・保存・独立processの
再読込を行う。最大深さ12、葉の最小sample数2、sqrt逆頻度をsample重みにする。
分岐閾値と葉のIDはデータから学び、教師を実行時の補助として呼ばない。
初版117ノードは同じ2501〜2503で1/3（MLP0/3、教師0/3）。次の未使用2601〜2605では
全5例で把持したが初回1/5・最終0/5（教師3/5）。把持後の上昇を横移動へ置き換える誤りが残った。

2601教師と2501/2601の木の実訪問状態のラベルを加えた23,121 sampleで149ノードへ再学習。
2701〜2705はこの学習に含まない。同seed比較では更新前の木0/5、更新後2/5、教師3/5。
探索2401〜2403では初回3/3・最終2/3となり、教師と同じ成功/保持失敗を示した。
2702では把持・閉指後にID7を反復する浅い葉（深さ5、訓練内のID7確率1.0）へ入り、
局面を誤る。直前の下降IDへの依存が見えたため、直前ID one-hotだけを削ったv6も
同じデータで比較したが、153ノード・同じ2701〜2705で最終2/5。削除単独では解決しなかった。
2705は教師IDとの不一致が1 stepだけで、腕経路拒否が405 step。教師にも失敗があり、
selector誤りと既存制御器/接近軌道の失敗が混在する。

接触を成功数と別に扱う。中間v5 MLPの2403では前腕と机に2 step、最大27.04 N、
2503の教師と初版木では上腕に各2/3 step、最大0.818 Nの接触を検出した。
有限サンプルの机経路検査は追従中の動的接触を完全には防がない。接触runも保存した。
149ノードの木の2401〜2403・2701〜2705では全記録stepの机接触0 Nだった。
この結果をすべての条件での安全保証とはしない。

今回の改善は既存20 IDから選ぶ教師ありselectorの改善であり、新しい身体制御器の獲得、
銀行追加、temporary蒸留・解放ではない。残る把持後の誤選択には反例状態の収集が必要で、
教師と判断がほぼ一致する経路拒否には局所制御の診断を分けて進める。
全run・失敗した学習・費用・最終固定評価は [実験メモ](ITERATION_LOG.md) に記録する。

最後に149ノードのv5モデルを固定して未使用2801〜2805で評価すると、学習器・教師とも
初回/最終2/5。学習器は2801/2804、教師は2804/2805で成功し、両方式とも机接触0 N。
2805の学習器は把持後に上昇を下降/閉指へ置換し、2802/2803は教師にも長い腕経路拒否がある。
同じ成功数でも同じ能力範囲とは言えない。この5条件の結果を見てからモデルを変更していない。
最終評価2801〜2805は学習sourceのseedと非重複を保存ログから確認した。

runner実績は16 run / 64 episode / 53,170環境step、episode wall合計629.4秒。
MLP4本で12,000勾配更新、CARTは3回fit。初回v5学習はfloat64/float32不一致で
最初の更新前に停止し、float32出力へ修正して別runで再開した。
実環境統合テストは既存1本をv5/v6対応時に各1回実行し、どちらも通過。
最終は **1 passed in 49.92s**（1,870 step、2回合計3,740テストstepはrunner総計外）。

## 15. 2026-09-24 把持状態の分割、回転介入と拒否原因の切り分け

前回の2701/2801での実訪問教師ラベル8,250件を加え、31,371 sampleで201ノードの木を
再学習した。探索2801〜2805では前回2/5に対し1/5へ低下。測定された`grasped`を根で分ける
構造上の事前設定 `--partition-grasp` も同じデータで181ノード・1/5だった。
この分割は教師stageではなく物理観測による。各枝の操作ID・以後の分岐は学習するが、
分割方法自体は手設計である。モデルを大きくしただけの改善とはしていない。

姿勢の誤判断が残り、2804では上昇を回転へ置換していた。木の分岐が物体の高さ・相対位置に
依存したため、450/520/590/660/730 stepで既存回転ID12/13を交互に1回選ぶ収集器を追加。
残りは固定学習器を使い、2804〜2806の実訪問2,599 stepへ教師を問い合わせた。
15回の介入、初回/最終1/3だが、manifestは`learned=false, exploration=true`とし、
学習器単独の成功には含めない。各stepに介入有無、介入前のモデルID、教師IDを分けて保存。
これを追加した33,970 sample・199ノードのモデルも、介入なしの2801〜2805で1/5だった。

追加学習モデルを固定して未使用2901〜2905へ移すと、初回3/5・最終2/5。
同じseedの前回149ノードは初回4/5・最終2/5、教師は初回4/5・最終3/5だった。
新モデルは2904/2905、旧モデルは2903/2904で最終成功。成功する配置が入れ替わり、
今回の方法が性能を改善したとは結論しない。2901〜2905は全学習sourceと非重複で、
結果を見てから再学習していない。前回モデルを比較基準として保持する。

**拒否の解釈を訂正:** 過去の表で広く「腕経路拒否」と呼んだreason2は、実装上は
`ik_or_joint_or_table_rejected`の合計である。2802/2803の旧教師の最初の拒否では
IK不成立・関節範囲違反があり、`ik_table_clear=true`だった。すべてを机衝突の拒否と
みなせない。新しい `scripts/analyze_primitive_runs.py` はIK・関節範囲・机経路の失敗flagを
重複可能な別件数で記録する。教師ラベルや接触が未計測の場合も未計測と0を区別する。

既存操作の組合せによる診断を3条件ずつ実行した。遠方移動後に腕拒否が5回続いたら
一度だけ2 cm後退する手設計列は、2802/2803で実際に後退したが最終0/3。
前方の並進が拒否された次stepに、目標へ近づく別の軸を先に動かす列も0/3。
2802/2803の拒否は旧教師431/448 stepから251/59へ減ったが、手先Xは約0.063 mに留まり、
物体X約0.095 mへ届かなかった。固定pitch15度を0度へ変える教師も0/3。
水平姿勢の2801では前腕と机の接触7 step、最大2.999 Nを検出した。
回復・軸順序・姿勢変更はいずれも手設計診断であり、新能力の学習には数えない。

今回の実績は10 runner run / 42 episode / 34,797環境step、episode wall合計474.3秒。
CART3 fit、勾配更新0。既存の統合テスト1本に把持の両状態からの学習、後退と軸変更、
介入ログを追加し **1 passed in 84.75s**（3,540テストstepはrunner総計外）。
水平姿勢診断以外の9 runは全記録stepの机接触0 N。全runと失敗を保存した。

次は同じ分類器へ相関したラベルを足し続ける前に、拒否目標に対する関節解・到達範囲と、
把持後の姿勢判断を別々に測る。IK失敗の初期状態から、到達可能な局所目標/姿勢の小さい
比較を作り、目標表現の制約か局所制御器の不足かを調べる。現時点では単純な後退・軸順序・
水平姿勢で解けず、既存集合で不可能とは証明していない。temporaryの局所制御学習、
銀行追加、圧縮・解放へ進んだ成果はまだない。詳細は [実験メモ](ITERATION_LOG.md) に残す。

## 16. 2026-09-24 同一失敗状態からの到達範囲とIK初期値比較

`probe_primitive_reachability.py`で最初の腕拒否を実環境から再現し、現在の関節・root姿勢・
要求目標・関節範囲・候補解を保存した。追加の物理stepを進めず、同じ位置での姿勢変更、
移動量半減、物体上の高さ変更、現在/リセット時の腕を初期値としたIKを比較する。
診断あり/なしの4,260送信actionは全件一致。保存した候補は合計1,000件で、運動学の
比較であり、候補数を環境stepやタスク成功へ数えない。

元の2802/2803の最初の前進拒否（step469/452）は、現在/リセット腕と胴体高さの
10初期値で元の目標を解けなかった。位置を維持して水平姿勢にすると4/6候補が制約を通過。
ただし物体上12/6/3/1.2 cm、pitch0/15/30度の目標では有効解が得られず、局所的に前進を
解けることと、把持点へ到達できることを区別した。この有限の探索で物理的不可能とは言わない。

台車から腕へ切り替える測定Xを.195→.215 mとして約2 cm追加前進した。pitch15度では
2801〜2803の最終0/3、拒否は机経路へ変わった。pitch0度も0/3で、2802の最初の拒否では
1 cm前進は解けないが5 mmなら有効解が4件あった。2803の下降拒否では、同じ目標に対して
リセット腕・胴体.2 mの初期値で有効解が1件あった。単なる距離不足だけではなく、移動量と
IKの初期値、机回避を切り分ける必要がある。

通常のIKと胴体固定fallbackが失敗した時だけ、腕の初期値を変えて最大5回解き直す
`--ik-reset-seed`を実装。物理状態や要求目標をresetせず、元の関節制限・机経路検査を通す。
同じ3条件で2803の3 stepを回復したが、最終成功・把持は0/3。別初期値の追加試行3,665回に
対して局所回復は3 stepであり、既定は無効のまま保持する。固定pitch5度も最終0/3だった。
水平姿勢の2803では4 step/最大.0609 N、5度姿勢では11 step/最大2.477 Nの接触を観測。
IK再試行runの接触0を安全保証やタスク能力の改善としない。

完了7 run/19 episode/15,060環境step、episode wall合計241.6秒。初回の診断保存は無限の
関節範囲をJSONへ直接書いて失敗し、969実行stepを残した。非有界範囲をnullと記録するよう
修正して別runで再開。失敗runを含む実行stepは16,029。学習更新0。実環境統合テストは
診断追加時に1回、IK再試行追加後に1回実行し、最終 **1 passed in104.93s**（4,930 step、
2回合計9,185テストstepは別計上）。全条件は探索済み2801〜2803で、独立評価ではない。

次は、5 mmなら解ける到達境界と、把持前の下降で姿勢・関節経路が机検査を通らない問題を
分けて扱う。失敗時の移動量縮小と、姿勢を接近/下降に合わせて変える経路を小さく比較する。
固定姿勢の変更や初期値再試行だけで解決したとはせず、学習selectorの再学習・新プリミティブ
獲得・temporary蒸留/解放も今回未実施。詳細なrunと再現検査は [実験メモ](ITERATION_LOG.md)。

## 17. 2026-09-24 拒否時の移動量半減、下降姿勢と把持高さ

並進ID0〜5の通常目標がIK・関節範囲・机経路のいずれかで拒否された場合だけ、同じ方向・
姿勢で距離を半分にして1回再試行する `--translation-backoff` を追加した。標準1 cmなら
再試行5 mmで、追従許容3 mmより小さくはしない。元の目標・最初のsolver結果・再試行目標・
採用有無を別々に保存する。最終reason2は再試行後も拒否されたstep数であり、最初の拒否数と
区別する。既定は無効で、固定20 IDの実行方法を変える手設計の回復である。

前回の追加前進2 cm・水平姿勢と同じ2801〜2803/900 stepでは、半減で4 stepを回復したが
把持・最終成功は0/3。半減前の合計1,877 actionは対照と全件一致した。全3条件で机接触を
観測し、運動学の制約を通過したことを動的な非接触保証にはできない。

接近時は0度、水平距離12 mm未満かつ物体上80 mm以下へ下降したら5度へ切り替え、以後
保持する手設計selectorを追加。`--descend-pitch-deg`で指定し、観測状態による切替と目標
pitchを記録する。半減と併用しても元の把持高さ12 mmでは0/3だった。高さ20 mmへ上げると
2803で把持を222 step観測したが、直前に手首と机が最大23.524 Nで接触し、持上げは拒否。
別初期値IKを併用してもこの失敗は同じで、2801の10 step回復だけでは成功を増やさなかった。

把持高さ25 mmでは同じ900 stepの全3条件で机接触0。2802で一時把持6 step、2803で224 step
把持し、後者は物体を最大約16.5 cm持ち上げて運搬へ進んだ。最終成功はまだ0/3で、2802は
物体を落とし、2801は下降姿勢への回転を机経路検査に拒否された。高さ20/25 mmを同じ
1,200 stepへ延長すると20 mmは0/3、25 mmは初回/最終1/3（2803、1,041 step）となった。
25 mm設定を固定した未使用3001〜3003では0/3、同条件の従来教師は3/3。今回設定は3条件とも
一時把持したが、持上げ量は最大3.6〜11.8 mmで把持を失った。腕拒否・机接触は全て0だった。
把持高さの変更は既知の難しい配置を1例改善したが、全体の性能改善とは結論しない。
これは学習selectorや新プリミティブ本体の獲得ではなく、手設計の到達・把持経路の診断である。

実環境統合テストは既存1本へ半減前後の目標保存、元の拒否、有効な再試行、観測による姿勢
切替と保持、把持高さのmanifest保存を追加し **1 passed in116.69s**（5,730テストstep、別計上）。
実験の成功率を合否条件にはしていない。全runと最終比較は [実験メモ](ITERATION_LOG.md) に記録する。

今回の実績は9 run/27 episode/26,251環境step、episode wall合計464.0秒、学習更新0。
3001〜3003の結果を見た後の設定変更・再学習は行っていない。既定のselectorと実行器は維持。
次は接触を避けつつ保持できる把持高さ・姿勢と、把持喪失後も指を閉じて待ち続ける既存教師の
回復動作を切り分ける。2801では姿勢切替時に未完了の下降目標も残っており、位置保持を
明示してから回転する比較も候補。失敗を全てIKやモデル容量の問題に戻さない。

## 18. 2026-09-24 上空姿勢切替、待機付き把持回復、把持高さの最適化

前回の課題（把持高さ25 mmでの滑り落ち、下降姿勢切替時の未完了並進目標による机拒否、把持喪失後の指閉じ硬直）を解決するため、手設計セレクターに3つの改善を導入した。

1. **上空姿勢切替（`--pre-rotate`）**: 手先がCube直近へ下降する前、上空（水平15 mm以内、高度12 cm）で下降pitch（10度）へ遷移させ、姿勢が整うまで下降を保留。未完了並進目標がある間の回転は抑止。これにより2801で起きていた姿勢切替拒否が解消した。
2. **閉指待機付き把持回復（`--recover-lost-grasp`）**: 閉指（ID 7）開始後20ステップの間は指を閉じ続け、その後も`grasped == False`または運搬中に落とした場合のみ開指（ID 6）して上空退避（Cube+12 cm）を発動し再アプローチする。
3. **適正把持高さと前傾（`--grasp-height-m .020 --pitch-deg 10 --descend-pitch-deg 10`）**: 12/16 mmの机干渉と25 mmの浅挟みの中間（Cube上面20 mm）に設定し、10度の前傾で手首の机接触マージンを確保。

結果として、難関seed 2801〜2803で3/3・3/3成功（全エピソード接触0 N、IK失敗0、机拒否0）、前回の滑り落ちseed 3001〜3003でも3/3・3/3成功（接触0 N、拒否0）を達成した。
さらに、完全新規の未知評価seed 3101〜3105（5 episodes）でも、全例接触0 N・拒否0で初回4/5・最終3/5成功を確認した。

これにより、Fetch遠方開始タスクにおける安定した手設計教師ベースラインが確立された。次は、この安定データを用いた学習selector（決定木/MLP）の再訓練、あるいはAPC本来の研究課題であるプリミティブ銀行・Temporary管理サイクルへの展開に進むことができる。

## 19. 2026-09-24 クリーン教師データによる自律判断器の再学習と未知seed自律完走

確立された安定ベースラインから、接触0・拒否0・再把持回復を含む高品質な14エピソード（計11,711ステップ）を統合し、自律セレクター（CART決定木およびMLP）を再学習した。

1. **学習器の特性と精度**:
   - CART決定木（145ノード、深さ12、把持状態分割）は検証精度**99.11%**を記録。
   - MLP（64x2、3,000更新）は検証精度84.81%にとどまり、実環境でも台車操作で拒否が多発。離散的なプリミティブ選択において決定木構造の優位性が明確となった。
2. **自律閉ループ推論（手設計補助なし）**:
   - 探索seed 2801〜2803において、決定木セレクター単独で**初回3/3（100%）、最終2/3（66.7%）、全例接触0.0 N**を達成（2801は691 step、2803は974 stepで完走）。
   - 完全新規の未知seed 3201〜3205（5 episodes）において、**4/5（80%）で把持・運搬まで自律到達し、2/5で完全成功**（同seed教師3/5に迫る水準）を達成した。

これにより、遠方開始タスク（FetchPickCubeFar）において、台車移動・上空姿勢調整・下降・把持・運搬に至る全行動シーケンスを、人間の手設計ルールに頼らず学習器単独で自律完走できることが実証された。
単一タスクの局所制御・選択最適化は本実験をもって完了とし、次はAPCコア研究であるプリミティブ銀行の自動構成、Temporary獲得・蒸留・解放サイクルの実装へと移行する。

## 20. 2026-09-24 APCプリミティブ銀行の最小実装とTemporaryライフサイクル実証

APC（Adaptive Primitive Composition）の中核仮説である「一次的なTemporaryモデルによる学習・探索 → 構造化Candidateへの蒸留・定着 → 一時リソースの完全解放・再請求 → 解放後モデル単独でのタスク再利用 → 過去能力の非破壊保持」の全ライフサイクルを実証する最小銀行基盤を実装し、実シミュレータ環境で一貫検証した。

1. **プリミティブ銀行の構成 (`apc_maniskill/primitive_bank.py`)**:
   - `BankEntry`: ID、名前、種別（temporary/consolidated/core）、モデル種別（mlp/cart/scripted）、パラメータ数、ファイルサイズ、SHA256ハッシュ、定着・解放統計を管理。
   - `PrimitiveBank`: メタデータ台帳（`bank.json`）とバイナリ保存領域（`primitives/`）を持ち、登録（`register`）、定着（`consolidate`）、一時モデル完全削除・監査（`release_temporary`）、完全性監査（`audit`）を提供。
2. **全自動ライフサイクル実証 (`scripts/run_bank_lifecycle.py` / `runs/bank-lifecycle-evidence-20260924-d`)**:
   - **Stage 1 (獲得 / Acquisition)**: Temporaryセレクター（MLP、11,092パラメータ、50,273 bytes）を登録。
   - **Stage 2 (定着 / Consolidation)**: 145ノードのCART決定木（勾配パラメータ0、21,220 bytes）へ定着。11,092パラメータ削減、29,053 bytes容量削減を記録。
   - **Stage 3 (解放 / Release)**: Temporaryファイルを物理削除し、メモリ・パラメータ・ストレージを100%解放（reclaimed: 11,092 params, 50,273 bytes）。
   - **Stage 4 (再利用 / Clean Reuse)**: 解放後の銀行からロードしたCandidate単独で、追加学習0・勾配更新0のまま新規未知seed 3201〜3203を実行。初回・最終 **2/3（66.7%）成功**（mean return 168.99）。
   - **Stage 5 (保持 / Retention)**: 過去の基準配置（seed 2801）を実行し、**1/1（100.0%）成功**（return 237.95）を維持。破滅的忘却の回避と非干渉性を実証。

本実証により、RESEARCH_PLANに掲げられた全6項目（身体性プリミティブの定義、毎step自律選択、未知seed汎化、Temporary獲得、Candidate定着と一時リソース完全解放、過去能力保持）が、実物理シミュレータ上で完結した。

## 21. 2026-09-24 多タスクプリミティブ銀行の拡張と異種タスク（Place）ライフサイクル実証

単一タスクのライフサイクル実証に続き、共通の20身体性プリミティブを用いつつ、異種タスク（運搬目標が+15 cmオフセットされた `APC-FetchPlaceCubeFar-v1`）を追加獲得し、銀行内に複数タスクのスキルを共存・保持させる多タスク実証を行った。

1. **環境拡張と対照実験**:
   - `APC-FetchPlaceCubeFar-v1`: Cubeの持ち上げ後、テーブル横方向（Y軸 +15 cm）のオフセット地点へ運搬・配置して静止するタスク。
   - 対照実験: Task A（Pick）のCandidateをTask B（Place）へ投入。幾何特徴によるアプローチ・把持は再利用されるものの、タスク特化の運搬目標への適応を検証（`--allow-task-mismatch` で測定）。
2. **Task B のTemporary獲得・定着・解放**:
   - 教師データ（seed 3001〜3003、2,433 steps、3/3成功）から、Task B 用のTemporary MLP（9,812パラメータ、44,769 bytes）を学習・銀行登録。
   - 57ノードのCART決定木（0パラメータ、11,300 bytes、検証精度96.6%）へ定着（9,812パラメータ削減、33,469 bytes容量削減）。
   - Temporaryモデルを物理削除し、メモリ・パラメータ・ストレージを100%解放（reclaimed: 9,812 params, 44,769 bytes）。
3. **多タスク自律実行と非破壊保持 (`scripts/run_bank_multitask.py` / `runs/bank-multitask-evidence-20260924-b`)**:
   - **Task B 解決**: 銀行から `fetch_place_v1` をロードし、追加学習0で seed 3001 を実行して **1/1（100.0%）完全成功**（837 steps、mean return 134.22）。
   - **Task A 保持**: 銀行から `fetch_pick_v1` をロードし、Task A（PickCubeFar, seed 2801）を実行して **1/1（100.0%）完全成功**（691 steps、mean return 87.89）を維持。
   - **銀行監査**: 銀行台帳（`bank.json`）に2つの定着済みスキル（Pick 21,220 bytes、Place 11,300 bytes）が共存し、SHA256ハッシュ完全一致で完全性を確認。

これにより、APCが複数タスクに対しても「身体性プリミティブを基盤とした逐次Temporary獲得 → 構造化Candidate定着 → リソース完全解放 → 複数スキルの銀行蓄積と非破壊即時再利用」というスケーラブルな適応サイクルを完全に実現できることが実証された。


