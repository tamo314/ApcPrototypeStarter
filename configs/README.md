# Configs

Reserved for experiment configuration files (e.g. `phase_a_smoke.yaml`), added starting with the milestone that first needs one (Milestone A2, fixed dense baseline). Empty as of Task 001 since no model exists yet.
